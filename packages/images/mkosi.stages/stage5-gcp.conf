# Stage 5: Full GCP build with postoutput
# Complete build including GCP packaging to identify packaging inconsistencies
# base/mkosi.conf provides kernel dependency
# tdx-kettle/mkosi.conf provides sqld dependency
# Note: GCP profile must be passed via --profile=gcp command-line argument

[Include]
Include=base/mkosi.conf
Include=tdx-kettle/mkosi.conf

[Output]
ImageId=tdx-debian-stage5
