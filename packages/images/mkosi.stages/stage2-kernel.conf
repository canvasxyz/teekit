# Stage 2: Base + kernel
# Builds the base image with kernel to identify kernel build inconsistencies
# This uses base/mkosi.conf which already includes kernel dependency

[Include]
Include=base/mkosi.conf

[Output]
ImageId=tdx-debian-stage2
