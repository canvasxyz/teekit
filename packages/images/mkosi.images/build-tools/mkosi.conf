[Output]
Format=directory
ImageId=build-tools

[Content]
# Build tools and dependencies for compiling applications
# This image is used only during the build process and not included in final images
BuildPackages=build-essential
              git
              curl
              cmake
              pkg-config
              clang
              cargo
              rustc
              ca-certificates
              nodejs
              npm
              python3
              flex
              bison
              bc
              elfutils
              libssl-dev
              libelf-dev
              perl
              gawk
              zstd
              kmod
# Runtime packages needed for build scripts in dependent images
# These are included in the BaseTree so they're available when other images use this as a base
Packages=bash
         cargo
         rustc
         git
         build-essential
         curl
         ca-certificates
         nodejs
         npm
         python3
